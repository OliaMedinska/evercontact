const item = document.querySelectorAll(".features__accordion-item");
const btn = document.querySelectorAll(".features__accordion-btn");
const icon = document.querySelectorAll(".features__accordion-polygon");